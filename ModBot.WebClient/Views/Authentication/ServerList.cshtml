
@model IEnumerable<GuildModel>
@{
    var AddDiscordBot = "https://discord.com/oauth2/authorize?response_type=code&client_id=844535789882834955&scope=bot%20applications.commands&permissions=2425564350&redirect_uri=https%3A%2F%2Flocalhost%3A5001%2Fauthentication%2Finitializeguild&guild_id=";
}
<div id="HeaderDiv">
    <label id="Header">Please select server</label>
</div>
<style>
    * {
        font-family: "Consolas";
    }
    #HeaderDiv {
        position: absolute;
        top: 5%;
        left: 27%
    }
    #Header {
        font-size: 80px;
        color: white;
        text-align: left;

    }
    #example1 {
        position: absolute;
        display: flex;
        width: 1250px;
        border-radius: 25px;
        border: 10px;
        padding: 40px;
        left: 19%;
        top: 20%;
        background: #2F3136;
        background-clip: border-box;
    }
    img {
        float: left;
        width: 70px;
        height: 70px;
    }
    #Text {
        color: white;
        padding-left: 15px;
        width: 400px;
        height: 10px;
        float: right;
        font-size: 20px;
        
    }
    #EditBot {
      width: 600px;
    }
    #meta {
        width: 1000px;
        display: flex;
        align-items: center;
    }
    #ButtonText {
        left: 50%;
        top: 50%;
        font-size: 25px;
    }
    #Button {
        position: relative;
        float: right;
        left: 50%;
        color: #fff;
        background-color: #44877F;
        font-weight: bold;
        width: 20%;
        height: 40px;
        letter-spacing: 0.05em;
        border-radius: 25px;
    }
    #order1 {
        order: 1;
    }
    #order2 {
        order: 2;
    }
</style>
<div id="example1">
    <table>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td style="padding-bottom: 10px;">
                        <div id="meta">
                            @if (item.HasBot == true)
                            {
                                <!-- Do something here to display your items in table format (or whatever format you want with as many columns you want -->
                                @if (item.Icon == null)
                                {
                                    <img src="~/images/MissingServer.png" alt="@item.Name" />
                                }
                                else
                                {
                                    <img src=@item.Icon alt="@item.Name" />
                                }
                                <p id="Text">
                                    @item.Name
                                </p>
                                <div id="Button">
                                    <a class="btn" id="ButtonText" asp-area="" asp-controller="Authentication" asp-action="EditGuild" asp-route-guildId="@item.Id">Edit bot</a>
                                </div>
                            }
                            else
                            {
                                @if (item.Icon == null)
                                {
                                    <img src="~/images/MissingServer.png" alt="@item.Name" />
                                }
                                else
                                {
                                    <img src=@item.Icon alt="@item.Name" />
                                }
                                <p id="Text">
                                    @item.Name
                                </p>
                                <div id="Button">
                                    <a href=@AddDiscordBot@item.Id class="btn btn-dark">Add bot</a>
                                </div>
                            }

                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
<p></p>